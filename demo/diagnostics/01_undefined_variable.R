# Demonstration of undefined variable warning

# Undefined variable — Raven flags this immediately
result <- undefined_var + 1

# Typo in variable name — caught before you run
my_data <- data.frame(x = 1:10)
summary(my_dta)
